# 安全政策

## 支持范围

当前维护分支为 `main`。安全修复优先进入该分支，不承诺为旧版本单独回补。

## 报告漏洞

不要在公开 Issue 中发布密钥、个人数据或可直接复现用户环境的日志。请使用 GitHub 仓库的私密漏洞报告功能，
提供受影响版本、最小复现步骤、预期与实际行为。提交前请用占位符替换所有敏感值。

## 密钥泄露

如果密钥已经公开，删除文件或提交并不能让密钥恢复安全。应先到相应服务撤销并轮换密钥，再清理公开内容和
Git 历史。发布前运行：

```sh
python3 tools/release_guard.py --root . --mode public
```

守门报告只输出规则、相对路径和修复提示，不回显检测到的密钥值。
