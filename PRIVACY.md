# 隐私说明

24h 留学助理采用本地优先的数据设计。学生画像、课程、作业、计划、进度和提醒状态默认写入用户自己的
Hermes Profile 数据库。仓库维护者不会因为用户安装本项目而自动收到这些数据。

## 数据范围

- 产品数据库包含用户主动确认的画像、学业数据、提醒策略和投递目标标识。
- Hermes 自己管理模型密钥、平台令牌、会话和记忆；本项目不将这些内容写入产品数据库。
- Vision 检查可能把自动生成的测试图片发送给用户选择的模型服务；运行前必须明确说明并取得授权。
- 当前没有截图 OCR 功能。未来实现后，截图中的每个条目都必须经用户确认才能持久化。

## 用户控制

用户可以用 `data export` 导出产品表，用 `data clear` 在精确确认后删除本产品数据。删除本地数据不会自动
撤销模型服务商或消息平台签发的密钥，用户需要在相应服务中单独处理。

## 公开协作

提交 Issue、测试证据或错误日志前，请移除真实姓名、学校、课程代码、截止日期、用户 ID、频道、Bot Token、
API 密钥、订阅地址和本机目录。发现公开内容含隐私信息时，请按 SECURITY.md 的私密方式报告。
